const DB_URI = 'mongodb://127.0.0.1:27017/';
const DB_NAME = 'properties';

const SR_ROOT_URI = 'https://api.simplyrets.com';
const SR_PROPERTIES_URI = `${SR_ROOT_URI}/properties`;
const SR_USR = "simplyrets";
const SR_PWD = "simplyrets";

module.exports = {
  DB_URI,
  DB_NAME,
  SR_PROPERTIES_URI,
  SR_USR,
  SR_PWD
}